  - name: Get number of bgp neighbors
    shell: "vtysh -c 'show ip bgp summary' "
    register: number_neighbors

  - set_fact:
      output: "{{ number_neighbors.stdout_lines | bgp_output_checker() }}"

  - debug: var=output
